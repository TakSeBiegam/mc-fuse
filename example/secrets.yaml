# secrets.yaml — Server-specific secrets
# Values here override globals from values.yaml.
# Encrypt with SOPS before committing:
#   sops --encrypt --age <pubkey> secrets.yaml > secrets.enc.yaml && rm secrets.yaml

SERVER_PORT: "25565"
SERVER_MOTD: "My Minecraft Server"
RCON_PORT: "25575"
RCON_PASSWORD: "change-me"
VELOCITY_SECRET: "change-me-velocity-forwarding-secret"
